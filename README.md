# tweet-intelligence-engine
AI-powered tweet intelligence app that generates tweets and predicts engagement using ML. app.py runs the Streamlit UI + model, tweet_generator.py handles generation, .pkl files store model &amp; encoders, and requirements.txt manages dependencies.
